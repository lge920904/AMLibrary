<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".KyungmanMainActivity">

    <Button
        android:id="@+id/mms"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignTop="@+id/contest"
        android:layout_centerVertical="true"
        android:layout_toEndOf="@+id/timer_sendmms"
        android:layout_toRightOf="@+id/timer_sendmms"
        android:gravity="bottom"
        android:text="SENDMMS" />

    <Button
        android:id="@+id/contest"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerVertical="true"
        android:layout_toLeftOf="@+id/timer_sendmms"
        android:layout_toStartOf="@+id/timer_sendmms"
        android:text="Contest" />

    <Button
        android:id="@+id/timer_sendmms"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignTop="@+id/mms"
        android:layout_centerHorizontal="true"
        android:text="timer_sendmms" />

</RelativeLayout>
